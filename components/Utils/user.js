import useSWR from "swr";

const fetcher = (url) => fetch(url).then((res) => res.json());


export const getUser = async (uid)=>{
    
    return {};
}


export const saveDBRecord = async (uid)=>{
    // TODO: replace firebase
    return {};
}

